services:
  - type: web
    name: process-io
    runtime: static
    buildCommand: echo "static site — no build step"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
